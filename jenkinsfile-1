/*pipeline {
    agent any
    tools {
        maven 'maven' // Name must match the Maven installation in Jenkins Global Tool Configuration
         
    }
    environment {
        SONARQUBE = 'SonarQube_Server' // Name of the SonarQube server in Jenkins configuration
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv("${SONARQUBE}") { 
                    sh 'mvn sonar:sonar'

                }
            }
        }
    }
}*/

pipeline {
  agent any
tools {
        maven 'maven' // Name must match the Maven installation in Jenkins Global Tool Configuration
         
    }
environment {
        JAVA_HOME = tool name: 'JDK 17', type: 'JDK'  // Set the correct JDK version
    }
 
  stages {
    stage('Scan') {
      steps {
        withSonarQubeEnv(installationName: 'SonarQube_Server') { 
         sh 'mvn sonar:sonar'
        }
      }
    }
  }
}
